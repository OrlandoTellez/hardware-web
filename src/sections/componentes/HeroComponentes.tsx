import React from "react"
import { Hero } from "@/components/Hero"

export const HeroComponentes = () => {
  return (
    <>
      <Hero 
      title="Componentes del Hardware" 
      subtitle="Descubra los componentes esenciales del hardware de un ordenador, desde la CPU hasta la memoria RAM, y cómo cada uno contribuye al funcionamiento del sistema." 
      />
    </>
  )
}
